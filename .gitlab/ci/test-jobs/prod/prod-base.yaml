.prod:test-base:
  stage: test-prod
  image: $DEV_IMAGE_LATEST
  tags:
    - dev
  needs:
    - build:prod-image
  before_script:
    - docker login -u "$CI_REGISTRY_USER" -p "$CI_REGISTRY_PASSWORD" "$CI_REGISTRY"
    - docker pull "$PROD_IMAGE"
