pip-audit:
  image: $DEV_IMAGE_LATEST
  services:
    - docker:29-dind
  tags:
    - dev
  extends: .pip-audit-base
  stage: security-dev
  needs:
    - test:registration
    - test:task
    - test:task_management_system
    - test:tms_auth
    - test:user
  script:
    - pip audit
