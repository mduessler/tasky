trivy-dev:
  extends: .trivy-base
  stage: security-dev
  needs:
    - test:registration
    - test:task
    - test:task_management_system
    - test:tms_auth
    - test:user
  script:
    - trivy config dev/Dockerfile.ci
    - trivy image --exit-code 1 --severity HIGH,CRITICAL $DEV_IMAGE_LATEST
