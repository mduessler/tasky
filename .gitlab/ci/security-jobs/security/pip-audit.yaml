pip-audit:
  extends: .pip-audit-base
  stage: security
  services:
    - docker:29-dind
  needs:
    - build:prod-image
  script:
    - docker run --rm --entrypoint pip $PROD_IMAGE_LATEST freeze | poetry run pip-audit -r /dev/stdin
