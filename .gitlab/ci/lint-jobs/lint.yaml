lint:
  stage: lint
  image: $DEV_IMAGE_LATEST
  needs:
    - build-dev-image
  variables:
    PRE_COMMIT_HOME: ${CI_PROJECT_DIR}/.pre-commit-cache
  cache:
    paths:
      - .pre-commit-cache/
  script:
    - pre-commit run --all-files
