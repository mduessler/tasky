plugin: amazon.aws.aws_ec2
regions:
  - "eu-central-1"
filters:
  tag:Project: "tasky"
  tag:Role: gitlab-runner
  tag:Environment: dev
  tag:RunnerName: "{{ gitlab_runner_name }}"
compose:
  ansible_host: instance_id
