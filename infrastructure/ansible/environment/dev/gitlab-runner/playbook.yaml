- name: Configure gitlab-runner
  hosts: all
  gather_facts: false
  pre_tasks:
    - name: Test SSM connection
      ansible.builtin.ping:
    - name: Gather facts
      ansible.builtin.setup:
  roles:
    - gitlab_runner
