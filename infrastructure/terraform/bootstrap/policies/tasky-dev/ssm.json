{
  "Version": "2012-10-17",
  "Statement": [
    {
      "Effect": "Allow",
      "Action": ["ssm:StartSession"],
      "Resource": "arn:aws:ec2:${region}:${account_id}:instance/*"
    },
    {
      "Effect": "Allow",
      "Action": ["ssm:TerminateSession"],
      "Resource": "arn:aws:ssm:${region}:${account_id}:session/${aws-user}-*"
    }
  ]
}
